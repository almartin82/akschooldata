# Get Alaska district reference data

Returns a data frame with Alaska school district names. Alaska has
approximately 54 school districts.

## Usage

``` r
get_ak_districts()
```

## Value

Data frame with district_id and district_name
